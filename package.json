{
  "name": "summit-up-design-system",
  "packageManager": "yarn@4.5.3",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "icons": "yarn workspace @summit-up/icons",
    "colors": "yarn workspace @summit-up/colors",
    "ui": "yarn workspace @summit-up/ui"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
